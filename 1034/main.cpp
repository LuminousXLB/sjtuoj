// 1034. 二哥的金链

#include <cstdio>

using namespace std;

int main(int argc, char const *argv[]) {
    int count, len;
    scanf("%d %d", &count, &len);
    for (int i = 0; i < count; i++) {
        
    }

    return 0;
}
