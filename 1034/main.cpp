// 1034. 二哥的金链

#include <cstdio>

using namespace std;

int * findmax(int * start, int len) {
    int max = start[0];
    for (int i = 0; i < len; ++i) {

    }
}

int main(int argc, char const *argv[]) {
    int count, len;
    scanf("%d %d", &count, &len);
    int *arr = new int[count];
    for (int i = 0; i < count; i++) {
        scanf("%d", &arr[i]);
    }



    delete[] arr;
    return 0;
}
